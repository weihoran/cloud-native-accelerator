spring:
    application:
     name: eureka-server 
    cloud:
        config: 
            uri: http://configserver:8071
            failFast: true
            retry:
                initialInterval: 8000
                multiplier: 1.3
                maxInterval: 5000
                maxAttempts: 20
